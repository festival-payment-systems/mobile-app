import {motion} from "framer-motion";
import {Box, Fab, Paper} from "@mui/material";

export const MotionBox = motion.create(Box);

export const MotionPaper = motion.create(Paper);

export const MotionFab = motion.create(Fab);